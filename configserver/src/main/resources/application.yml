server:
  port: 8980
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/emreokumus1994/spring_cloud_config_server
    profiles:
      active: native